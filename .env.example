DB_USER=bloguser
DB_PASSWORD=blogpass
DB_NAME=blogdb

# Leave commented out for local development (docker compose will build both
# images locally). On the server, uncomment and point these at the images
# GitHub Actions pushes to GHCR so deploy.sh can pull instead of rebuild.
# BACKEND_IMAGE=ghcr.io/NetanTech/simple-blog-backend:latest
# FRONTEND_IMAGE=ghcr.io/NetanTech/simple-blog-frontend:latest
